export enum OverlayLvl {
  /**
   * - Page overlay base level behind settings;
   * - Settings button;
   */
  Z100 = 100,
  /**
   * - Page overlay base level on top of settings;
   */
  Z101 = 101,
}
